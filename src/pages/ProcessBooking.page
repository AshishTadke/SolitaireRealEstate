<apex:page controller="ProcessBooking" action="{!Process}">
    <apex:form id="fm1">
        <apex:pageMessages >
        </apex:pageMessages>
           <apex:actionPoller action="{!incrementCounter}"  interval="5" rendered="{!IF(status == true, true, false)}"/>
    </apex:form>
</apex:page>